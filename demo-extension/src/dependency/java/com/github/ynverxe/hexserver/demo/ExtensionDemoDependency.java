package com.github.ynverxe.hexserver.demo;

import com.github.ynverxe.hexserver.extension.HexExtension;
import org.jetbrains.annotations.NotNull;

public class ExtensionDemoDependency extends HexExtension {
  public ExtensionDemoDependency(@NotNull Object context) {
    super(context);
  }
}
